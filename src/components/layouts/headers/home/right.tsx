import { FC } from 'react'

import { Button } from '@ui'

const Right: FC = () => (
  <>
        <Button palette='success' size='s'>
      Sign in
    </Button>
    <Button palette='inherit' variant='bgless'>
      Support
    </Button>
  </>
)

export default Right
